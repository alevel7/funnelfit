export enum ClientRequestStatus {
  NEW = 'new',
  DATE_DUE = 'dateDue',
  COMPLETED = 'completed',
  DECLINED = 'declined',
  ACCEPTED = 'accepted',
  SCHEDULED = 'scheduled',
}

export enum MeetingMode {
  IN_PERSON = 'IN_PERSON',
  PHONE_CALL = 'PHONE_CALL',
  VIDEO_CALL = 'VIDEO_CALL',
}
